name: Build macOS Application

on:
  push:
    branches: [ develop/v1_mac ]
  workflow_dispatch:

jobs:
  build-macos:
    runs-on: macos-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Set up Python 3.13
      uses: actions/setup-python@v4  # v5からv4に変更
      with:
        python-version: '3.13'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller
        
    - name: Build with PyInstaller
      run: |
        pyinstaller --onefile app.py --name akio-local-engine
        
    - name: Upload macOS Application
      uses: actions/upload-artifact@v2  # v3からv2に変更
      with:
        name: akio-local-engine-macos
        path: ./dist/akio-local-engine
